<h1>Listing Names</h1>

<%= link_to 'New Name', new_name_path %>

<table>
	<% @names.each do |name| %>
		<tr>
			<td><%= name.name %></td><br>
			<td><%= link_to "Click here to see your name reversed!", name_path(name) %></td>
		</tr>
	<% end %>
</table>